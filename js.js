// UX Google material style

var UEX = UEX || {};

UEX.prototype.init = function(){
  // find elements by specific class name
  // attach event listeners to this elements
  this.controledElements = document.getElementsByClassName('uex');
}
